{
  "name": "pulse-stroll-pages",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "cf:project:create": "wrangler pages project create pulse-stroll-web",
    "cf:whoami": "wrangler whoami",
    "cf:deploy": "wrangler pages deploy build/web --project-name pulse-stroll-web",
    "cf:deploy:preview": "wrangler pages deploy build/web --project-name pulse-stroll-web --branch preview"
  },
  "devDependencies": {
    "wrangler": "^4.14.1"
  }
}
